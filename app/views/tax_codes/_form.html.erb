<%= form_for(@tax_code) do |f| %>
  <% if @tax_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tax_code.errors.count, "error") %> prohibited this tax_code from being saved:</h2>

      <ul>
      <% @tax_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
